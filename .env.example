# =============================================================================
# ENVIRONMENT VARIABLE TEMPLATE FOR "QuestByCycles" FLASK APPLICATION
# =============================================================================

# -----------------------------------------------------------------------------
# === 1. MAIN (non-sensitive defaults can also be overridden here) ===
# -----------------------------------------------------------------------------
UPLOAD_FOLDER=images
VERIFICATIONS=verifications
BADGE_IMAGE_DIR=badge_images
TASKCSV=csv
LOCAL_DOMAIN=localhost:5000

# -----------------------------------------------------------------------------
# === 2. DEFAULT SUPER ADMIN (SENSITIVE) ===
# -----------------------------------------------------------------------------
DEFAULT_SUPER_ADMIN_USERNAME=
DEFAULT_SUPER_ADMIN_PASSWORD=
DEFAULT_SUPER_ADMIN_EMAIL=

# -----------------------------------------------------------------------------
# === 3. FLASK SECRET & SESSION SETTINGS (SENSITIVE) ===
# -----------------------------------------------------------------------------
SECRET_KEY=
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_NAME=QuestsByCycles_Session
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_DOMAIN=   # Set to domain on production
SESSION_REFRESH_EACH_REQUEST=true
REMEMBER_COOKIE_DURATION_DAYS=7

# -----------------------------------------------------------------------------
# === 4. FLASK-SQLALCHEMY DATABASE URI (SENSITIVE) ===
# -----------------------------------------------------------------------------
SQLALCHEMY_DATABASE_URI=

# -----------------------------------------------------------------------------
# === 5. OPENAI (SENSITIVE) ===
# -----------------------------------------------------------------------------
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# === 6. MAIL SERVER (SENSITIVE) ===
# -----------------------------------------------------------------------------
MAIL_SERVER=localhost
MAIL_PORT=2525
MAIL_USE_TLS=false  # Set to true for production
MAIL_USE_SSL=false
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=info@questbycycle.org

# -----------------------------------------------------------------------------
# === 7. SOCIAL API KEYS & TOKENS (SENSITIVE) ===
# -----------------------------------------------------------------------------
TWITTER_USERNAME=
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_TOKEN_SECRET=

FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
FACEBOOK_ACCESS_TOKEN=
FACEBOOK_PAGE_ID=

INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_USER_ID=

# -----------------------------------------------------------------------------
# === 8. SOCKET.IO SERVER (Override if needed) ===
# -----------------------------------------------------------------------------
SOCKETIO_SERVER_URL=ws://127.0.0.1:5000
# -----------------------------------------------------------------------------
# === 9. TWA CONFIGURATION ===
# -----------------------------------------------------------------------------
TWA_SHA256_FINGERPRINT=
# =============================================================================
